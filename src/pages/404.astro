---
import Base from "../layouts/Base.astro";
import { notFound } from "../content/pages";
import { notFoundEs } from "../i18n/es";
import { practice } from "../content/practice";
import { seo } from "../content/seo";
---

<Base title={seo.notFound.title} description={seo.notFound.description} noindex>
  <section class="px-3 pb-1.5 pt-28 md:px-5 md:pb-2 md:pt-36" aria-label="Page not found">
    <div
      class="flex min-h-[60vh] flex-col items-center justify-center rounded-xl bg-sky px-5 py-16 text-center md:rounded-2xl md:px-10 md:py-24"
    >
      <p class="font-display text-[clamp(4rem,14vw,9rem)] font-bold leading-none tracking-tight">
        404
      </p>
      <h1 class="font-display mt-4 max-w-2xl text-2xl font-bold tracking-tight md:text-4xl">
        {notFound.heading}
      </h1>
      <p class="mt-3 max-w-md type-body text-ink/65">
        {notFound.sub}
      </p>
      <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
        <a
          href="/book"
          class="inline-flex items-center rounded-full bg-cobalt px-6 py-3.5 text-sm font-bold text-white transition-colors hover:bg-cobalt-deep motion-safe:hover:scale-105 motion-safe:transition-[transform,background-color]"
        >
          {notFound.bookCta}
        </a>
        <a
          href="/"
          class="inline-flex items-center rounded-full border border-ink/30 bg-white px-6 py-3.5 text-sm font-bold transition-colors hover:bg-navy hover:text-white"
        >
          {notFound.homeCta}
        </a>
      </div>
      <a
        href={`tel:${practice.phone.tel}`}
        class="mt-6 text-sm font-semibold text-ink/65 underline-offset-4 hover:underline"
      >
        Or call us: {practice.phone.display}
      </a>
      <p class="mt-8 max-w-md text-sm text-ink/65" lang="es">
        {notFoundEs.heading} {notFoundEs.sub}
        <a href="/es/" class="font-semibold text-cobalt underline-offset-4 hover:underline">
          {notFoundEs.homeCta}
        </a>
      </p>
    </div>
  </section>
</Base>
